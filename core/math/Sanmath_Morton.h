#pragma once
// Morton moved to the canonical MATH module (Work-Order M0-1).
// This header forwards so existing includers keep compiling; new code should
// include "src/math/Morton_MATH.h" directly. Single definition lives there.
#include "../../src/math/Morton_MATH.h"
